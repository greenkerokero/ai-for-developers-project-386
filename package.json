{
  "name": "booking-api",
  "version": "1.0.0",
  "description": "TypeSpec API for a meeting booking system",
  "type": "module",
  "devDependencies": {
    "@typespec/compiler": "^0.65.0",
    "@typespec/http": "^0.65.0",
    "@typespec/rest": "^0.65.0",
    "@typespec/openapi3": "^0.65.0",
    "@typespec/openapi": "^0.65.0"
  },
  "scripts": {
    "compile": "tsp compile .",
    "watch": "tsp compile . --watch"
  }
}
